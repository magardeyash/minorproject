import { EmployeeLoginForm } from "@/components/auth/EmployeeLoginForm"

export const metadata = { title: "Contributor Login — VentureLens" }

export default function EmployeeLoginPage() {
  return <EmployeeLoginForm />
}
